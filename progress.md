# Progress — uqfusion

> Purpose: a cold read of this file (plus `scope.md` and `docs/plan-2026-07-07-kickoff.md`) must reconstruct where the project stands without re-deriving anything. Updated at every phase boundary and every non-trivial decision.

**Last updated:** 2026-09-04 (**The night-veto axis is CLOSED after three pre-registered attempts, and the veto stands unchanged.** V1 (remove it), V2 (gate on VIS health), V3 (health as sole authority) all ran; none adopted. The durable account is [`docs/night-veto-axis-closed-2026-09-04.md`](docs/night-veto-axis-closed-2026-09-04.md) -- the `runs/eval/` reports are untracked. **The real finding is not the rule:** on a night-capable detector, removing the veto is worth **+0.1785** on a clean night cell (0.0850 -> 0.2635, 33x the floor), but the same change costs **-0.0737** when VIS is fogged. The veto gates on DARKNESS; what predicts whether VIS should be dropped is VIS HEALTH. **The open question is the detector, not the gate** -- priced in [`docs/rebaseline-proposal-2026-09-04.md`](docs/rebaseline-proposal-2026-09-04.md). Prior update 2026-09-03 (**The night labels were the bug.** VIS was never blind at night, it was untrained there: restoring the boxes `filter_night_boxes.py --cut-dark` deleted takes night VIS **0.0000 -> 0.2520** ([`docs/experiment-log-2026-09-02.md`](docs/experiment-log-2026-09-02.md) 7.2). `veto_vis` fires on 100% of night frames on a justification that no longer holds, so **every night fusion cell in this project is `ir_only` vs `ir_only`** and is uninformative about fusion. Two pre-registrations were written before touching anything: [`docs/prereg-uq-day-night-slice.md`](docs/prereg-uq-day-night-slice.md) (`a4f9364`) and [`docs/prereg-night-veto.md`](docs/prereg-night-veto.md) (`5761e9f`). Prior update 2026-08-20 (**Architecture finalized for full-scale training** — [`docs/architecture-final-2026-08-20.md`](docs/architecture-final-2026-08-20.md), decisions D27–D30. The 2026-08-19/20 fusion screening rounds are recorded in [`docs/fusion-gate-experiment-record.md`](docs/fusion-gate-experiment-record.md) + [`docs/followup-analysis-2026-08-20.md`](docs/followup-analysis-2026-08-20.md): the night-fusion claim is retired, the system reframes as **sensor selection** — capability-weighted WBF + hard photometric veto with dilate-15 hysteresis; IR full-scale recipe = `yolo26s-p2feat` @640. Prior update 2026-08-17: Phase 1 benchmark grid **COMPLETE, audited and consolidated** — 93 rows across two campaigns in `phase1_benchmark/results.csv`; repo cleanup: `main` fast-forwarded to the phase-1 record branch, 8.6 GB of verified-redundant archives removed, the two dated Phase 1 handoffs folded into `docs/phase1-experimental-record.md`)
**Current phase:** Phase 1 — **GPU portion COMPLETE**; Table 1 selection memo is the remaining deliverable. Phase 3's CPU-verifiable portion is also COMPLETE with the smoke gate GREEN (`scripts/smoke_phase3.py`; see Run log) — MC-Dropout, Deep Ensembles, prediction caches, corruption generation, the pre-registered calibration suite, learned gate and the gate-ablation sweep are all implemented, and the three eval CLIs are verified. **`HOW_TO_RUN.md` is the canonical per-phase runbook.**
**Phase 2 — CPU portion COMPLETE** (commit `e75c110`): O2→O3→O4 chain green on both gates; server-side remainder = real-data run + §12.1 parity check + real homography (OQ-5).
**Backbone selected 2026-08-17: `yolo26m`, trained on the full-resolution twin (D25).** Because YOLO26 is end2end with `reg_max=1`, the σ² head needed a port — **done and gated (D26)**; the σ path now supports both head families, so §7.2 (OQ-10) can still be answered either way. **The remaining blockers are decisions, not code:** OQ-10 (§7.2), OQ-11 (`imgsz`), OQ-12 (host + ensemble budget).
**Phase 1 — COMPLETE** (grid finished 2026-08-17, `cc686ee`). 9 variants × 3 seeds on the `main` split plus 66 merged `pilot` rows = 31 variants. **The headline is negative and that is the result:** the top eight variants span 0.0055 mAP50-95 and `yolo26x`'s 0.0016 lead is below its own seed sd — not separable. The one robust finding is the capacity floor (every n/t-scale model 0.2486–0.2567). Two defects dominate the methods section: ultralytics 8.4.7 reports mAP ~0.034 higher than 8.4.90 for identical weights, and a shared run directory collided two `yolo12x` processes. **Full account: [`docs/phase1-experimental-record.md`](docs/phase1-experimental-record.md).** Still open before publication: `yolo12x` seed 1 is inadmissible and unreproducible (~17.5 h to retrain), and `yolo26m` seed 0 is the lone 8.4.7-trained row.
**Phase 0 — COMPLETE** (commit `ffbe87f`): repo, pinned env, portable config, docs.
**Session 2026-09-04 — five things a cold reader must know:**
1. **The night veto stands, and the axis is closed by a registered stop rule.** Three
   registrations ran in one day: [`docs/prereg-night-veto.md`](docs/prereg-night-veto.md)
   (V1, `5761e9f`), [`docs/prereg-night-veto-v2.md`](docs/prereg-night-veto-v2.md)
   (V2, `c9a215c`), [`docs/prereg-night-veto-v3.md`](docs/prereg-night-veto-v3.md)
   (V3, `0890a1a`). V1 INCONCLUSIVE (2 night cells below `ir_only`); V2 INCONCLUSIVE
   (5 night cells below `max(VIS, IR)`); V3 VOID (day veto 0.8% -> 19.9% on one cell).
   V3 §6 fixed the stop rule **before** its result was known, precisely because each
   round removed exactly the obstacle the last one hit. **There is no V4.**
2. **V3 solved the night completely and broke the day doing it, and the mechanism is
   measured.** On day frames with a glared IR, `ir_night_raw` fires on **19.9%** of
   clear days; the **authority bound** is what cuts that to 0.6%. V3 re-admits the
   rejected calls and asks health to confirm them instead. With VIS clean, health
   blocks it (0.0%); with VIS `lowlight`, health *confirms* it (100% -> 19.9% day
   veto). **The two votes stopped being independent** -- "VIS is unhealthy" is equally
   true of a corrupted daylight frame and of a real night frame.
3. **`vis_health` was broken and one line of fitting fixed it.** As shipped it flags
   100% of night frames unhealthy in *every* condition including clean, with the
   ordering inverted (fogged night 0.1931 vs clean night 0.0254), because it is fitted
   on FIT_RUNS which excludes `pohang01`. Pool 782 clean night frames into its
   reference and the same statistic reaches **AUROC 0.9921** -- and the reference and
   evaluated frames are disjoint (`maha_fit_vis` vs `paired_val_vis`, zero overlap).
   The refit is screening-only and is **not** written to `structure_constants.json`.
4. **The VIS ensemble seeds on dgxanode01 are void; the ep25 benchmark is clean.**
   `ens_vis_seed{1..4}_ft` have `args.yaml` at 2026-08-25..28, eight to ten days before
   the server labels were restored (2026-09-03 17:00 UTC; verified 18,826 files /
   96,584 boxes / hash `43ee6078395c` PASS, matching this laptop). They encode "night
   is empty", so **Stage B of the UQ slice cannot use them**. The benchmark's first run
   starts 2026-09-03 18:06 UTC, an hour *after* -- all 93 runs are on correct labels
   and that campaign does not need restarting.
5. **NLL was reporting the clip constant, not the model.** 13 of 34,776 VIS MC edges
   (0.037%) carried **100.0000%** of a 1.891e16 NLL: `cluster_records` emits sigma
   exactly 0 when two members agree, where the Gaussian NLL is +inf, and `_EPS = 1e-9`
   rendered that infinity as ~1e17 per edge. `gaussian_nll` now returns **NaN**. No
   published number moves (the sigma-head arms have no such edges, and NLL was already
   NO-SIGNAL in the slice). **No floor was adopted** -- `runs/eval/nll_floor.md` shows
   none is neutral: 53% of VIS sigma-head edges sit below 0.5 px.

**Session 2026-09-03 — three things a cold reader must know:**
1. **The dataset silently reverted mid-session and nothing explains it.** 7,591 `pohang01`
   train label files were rewritten to their pre-filter content at 21:19, restoring 18,772
   boxes that the per-box audit had dropped. No process of this project writes labels; the
   terminal was clean; no Python was running. Recovered (94,553 kept / 38,135 dropped,
   all-VIS hash `b92739202127`), and `scripts/train_night_fullscratch.py` now refuses to
   start unless the hash matches — that gate is the only thing standing between this and a
   wasted 14 h run. **Cause still unknown (OQ-13).**
2. **There is no git remote.** `git remote -v` is empty; this repo has only ever existed on
   the laptop, and the dgxanode01 copy is a manual copy that was never a clone. That is why
   `restore_night_perbox.py` is absent there and why labels had to ship as a zip. Every
   commit in this project lives in exactly one place (OQ-14).
3. **Server paths in older docs are wrong.** dgxanode01 is `/workspace/uqfusion`,
   `/workspace/pohang/visible`, `/workspace/derived` — not `Saroha_Work`, not
   `Pohang_dataset/`, not `runs/derived/`. `verify_dataset_state.py` errors there because
   `config.yaml` points at a laptop path.

**Session 2026-09-04 (continued) — VIS UQ retrain queued, six things:**
1. **The U1 amendment ran on the night-capable VIS checkpoint — CONTAMINATED, but
   confounded.** [`docs/prereg-uq-day-night-slice-amendment-nightfull.md`](docs/prereg-uq-day-night-slice-amendment-nightfull.md)
   declared the `gauss_vis_nightfull` swap as its own question before running it (no
   silent re-run). Result in `runs/eval/uq_day_night_slice_nightfull.md`: VIS
   CONTAMINATED (`d_ece` r=1.03), IR SUSPECT. But the VIS MC-Dropout arm
   (`mc_vis_seed0_ft_refit`) is still trained on the pre-restore labels and is hollow
   at night by direct count (**28** detections / 1,032 night frames vs the sigma-head's
   **8,391**) — the CONTAMINATED verdict is driven by that population mismatch, not by
   two comparably-populated arms diverging under restored labels. One clean signal
   survives the confound: on the seeing checkpoint, `d_ece` degrades day→night
   (0.1091→0.2779) while `aurc` *improves* (0.5219→0.4110) — not simply "worse
   everywhere." Along the way, fixed a real pre-existing crash in
   `scripts/slice_uq_day_night.py` (`KeyError: 'NO-SIGNAL'` when a metric's day-subset
   separation is exactly 0 — floor degenerates and `sep ≥ floor` alone isn't enough).
2. **Two more VIS UQ arms are night-blind the same way `gauss_vis_seed0_ft` was**:
   MC-Dropout (`mc_vis_seed0_ft_refit`, trained 2026-09-01) and Ensemble×5
   (`ens_vis_seed0_ft_control` local + `ens_vis_seed{1..4}_ft` on dgxanode01, trained
   2026-08-25..28) — all predate the 2026-09-03 label restore.
3. **Full VIS retrain queued**, `runs/queue_vis_uq_retrain/queue.json`, 8 runs
   sequential on the restored labels (`data_vis_stride2.yaml`, hash `b92739202127`),
   ~75 GPU-h total: `mc_vis_nightfull` → `gauss_vis_seed0_nightfull` →
   `gauss_vis_seed0_nightfull_ft` → `ens_vis_nightfull_seed{0..4}`. The gaussian pair
   (priced exactly from `gauss_vis_seed0`/`_ft`'s own measured `results.csv`: 13.65h +
   2.68h = 16.33h) was inserted mid-run ahead of the ensemble seeds because
   `gauss_vis_seed0_ft` — not the diagnostic `gauss_vis_nightfull` — is the checkpoint
   actually wired into the shipped `crossmodal26m` fusion pipeline and every night-veto
   registration (V1-V3) scored it while it was night-blind (2 detections/1,032 night
   frames). All cold-started (not continued) so the resume-safe runner can't silently
   resume a blind checkpoint under a shared name. Only `gauss_vis_seed0_nightfull` gets
   the mosaic-off ft stage — see the Q&A above for why MC/ensemble don't. Full rationale
   and restart/monitoring commands: [`docs/handoff-2026-09-04-uq-retrain.md`](docs/handoff-2026-09-04-uq-retrain.md).
   Dashboard at `http://127.0.0.1:8771`.
4. **Operational incident, caught and fixed**: pausing the running queue to insert the
   gaussian pair did not stop the OS process — `pause` blocks it in a polling loop
   rather than exiting it. Starting a second `run` process and calling `resume` woke
   both simultaneously (two live PIDs against the same queue-dir, confirmed via
   `Get-CimInstance Win32_Process`). Fixed by killing the stale PID; verified the
   survivor was training cleanly (not corrupted) via `nvidia-smi` (98% util, 11.17 GB)
   and console log inspection. No lasting damage, but `pause` ≠ process exit is now a
   known trap for this runner.
5. **PR #1 opened**: [github.com/Laksh-saroha/uqfusion/pull/1](https://github.com/Laksh-saroha/uqfusion/pull/1),
   `fusion-uq-phase3` → `main` (88 commits, ~44k lines — the branch had never been
   PR'd before today).
6. **Correction to 2026-09-03's item 2 below**: a git remote *does* exist —
   `origin` = `github.com/Laksh-saroha/uqfusion.git`, public, confirmed today. The
   2026-09-03 claim ("no git remote... has only ever existed on the laptop") was wrong
   even at the time it was written, not just stale.

**Phase 4 — NOT STARTED** (needs go-ahead + Phase 1–3 server results): Table 3 runner, real VIS↔IR pairing, both-degraded row + R_sys histogram, MIT in, DETR row decision, multi-seed publication runs.

---

## Cold-start pointers

| File | Role |
|---|---|
| `scope.md` | Source of truth for the science (architecture, datasets, eval plan) |
| `docs/plan-2026-07-07-kickoff.md` | Approved (2026-07-07, unedited) architecture review + backbone selection plan; all decision rationale lives there |
| `progress.md` (this file) | Current state, decision log, open questions, run log |
| `config.yaml` | The one file edited when moving machines; all paths + hyperparameters |
| `HOW_TO_RUN.md` | Canonical per-phase runbook — the commands, verified |
| `phase1_benchmark/README.md` | **The Phase 1 record**: layout + column dictionary. Read before touching `results.csv` |
| `docs/phase1-experimental-record.md` | Phase 1 provenance, defects, corrections, operational traps, measured VRAM limits, limitations text. Absorbed both dated handoffs |
| `docs/phase1-pilot-grid.md` | Why the two campaigns' splits differ and what merging them assumes |
| `docs/dataset-changes-2026-07.md` | Every dataset mutation since kickoff, with rationale |
| `docs/ir-handoff-2026-08.md` | IR modality state |
| `docs/fusion-gate-experiment-record.md` + `docs/followup-analysis-2026-08-20.md` | The 2026-08-19/20 fusion screening rounds: everything tried, refuted, and kept |
| `docs/architecture-final-2026-08-20.md` | **The frozen full-scale architecture** (D27–D30), pending gates, baseline policy, publication framing |
| `docs/TODO-2026-08-20-full-scale.md` | **The current TODO list** — launch blockers, pre-publication checks, the training matrix, integrity items. Supersedes `TODO-improvements.md` |
| `archive/phase1/README.md` | What survives of the two server pulls, and why each file is kept |

## Working agreement (from kickoff, 2026-07-07)

- Collaboration mode: senior-engineer peer — push back with evidence, don't rubber-stamp.
- Plan → Laksh reviews/pushes back/approves → implement, **one phase at a time**; Laksh gives an **explicit go-ahead after each completed phase** before the next starts (restated 2026-07-07).
- **No GPU in the dev environment.** CPU smoke tests only here; all real training on Laksh's GPU Jupyter server.
- Portability: no absolute paths anywhere; machine specifics live in `config.yaml` only.
- Every trainable component ships a tiny-subset smoke path before any GPU time (scope §18-2 discipline, applied everywhere).
- Git commit at every phase boundary (scope §11.1: which commit produced which result).

## Decision log

| # | Date | Decision | One-line rationale | Source |
|---|---|---|---|---|
| D1 | 2026-07-07 | §7.2: Gaussian σ² branch **added alongside** DFL (option c); DFL-derived uncertainty = free same-model ablation row; full DFL replacement = fallback only. Final call after Phase 1 winner is known. **Amended per A4-11 (approved conditionally):** branch retained iff it (i) doesn't degrade detection (§12.1 check) and (ii) yields calibrated, useful σ² — else fall back to option (b). Interpretation on record: "drop σ² entirely" is not an option since the branch *is* O2, the core contribution — Laksh to flag if he meant something stricter. | Single-model ablation, preserves deterministic baseline and §4 attribution | plan B1; scope §7.2; answer A4-11 |
| D2 | 2026-07-07 | §7.3: evidential regression cited, **not benchmarked**; head/loss built as pluggable interface | Doesn't test Comparison 2's claim; second custom head ≈ weeks off critical path | plan B2; scope §7.3 |
| D3 | 2026-07-07 | §7.4: keep caveat + both-degraded Table 3 row; **add absolute `R_sys` abstain signal** | Failure is weight normalization discarding magnitude, not missing signals | plan B3; scope §7.4 |
| D4 | 2026-07-07 | §7.5: learned gate included as upper bound, Phase 3, tiny logistic/MLP on existing features | Cheap ceiling comparison; supervision target TBD in Phase 3 | plan B4; scope §7.5 |
| D5 | 2026-07-07 | §6.4 constants: every constant = pre-registered statistic of clean validation data; **all gate ablations on cached predictions** (CPU post-processing) | Reproducible, no hand-tuning, ablation table nearly free | plan B5; scope §6.4 |
| D6-rev | 2026-07-07 | **Revised per A2-4/A2-5, replacing D6's defaults:** Laksh's existing custom split is used (not the D6-default block-builder, not an official PoLaRIS split); pohang01 (night) **is allowed in training** (reverses D6's full hold-out). Leakage-freedom is not assumed — it is verified mechanically by `scripts/audit_split.py` (cross-split duplicates + within-run temporal buffer, ~10 s), which **must exit 0 before any training**. Consequences: Table 3's low-light column is primarily synthetic darkness; test split should include pohang01 blocks to preserve a real-night eval row (requested in dataset_requirement.md §3). | 10 Hz video → random splits leak near-duplicates into every table; audit replaces trust | answers A2-4/A2-5; plan B6-2, C5 |
| D7 | 2026-07-07 | Fusion coordinate frame: static IR→VIS homography from dataset calibration; fuse + evaluate in VIS frame | WBF needs a common image plane; scope §5.1's "no registration needed" is too strong | plan B6-9 — pending A2-8 |
| D8 | 2026-07-07 | Table 1 shortlist: YOLOv8s/v9s/v10s/11s/12s/26s, s-scale, seeds {0,1,2}, Pohang VIS + winner/runner-up IR confirm; selection rule pre-declared (within ~1 std of best mAP@50–95 → DFL > simplest fork > FPS). **Confirmed by A4-12** (no expansion to a full ×2-modality grid; IR rows can be backfilled later with a config flip if the manuscript wants them). | All Ultralytics-native = one config/API; rule prevents post-hoc choice | plan C2/C4/C5; scope §8, §9.1; answer A4-12 |
| D9 | 2026-07-07 | DETR row deferred to Phase 4 | Gates nothing; identical-config unfair to DETRs; that SOTA churning (RF-DETR/DEIMv2/RT-DETRv4) | plan C3; scope §8 |
| D10 | 2026-07-07 | MIT Marine Perception deferred to Phase 2+ parallel onboarding; hard requirement by Phase 4 | Annotation pipeline is a named risk (§13); keep it off the Phase 1 critical path | plan C4; scope §5.1, §13 |
| D11 | 2026-07-07 | Phases: added Phase 0 scaffold; benchmark harness built in Phase 1 (not §18 step 5). **Phase 1/2 decoupled (Laksh, same day):** "Just move on to phase 2. I will run the server test later" — Phase 2 CPU-side work proceeds now; Phase 1's GPU portion (audit → grid → Table 1) runs whenever Laksh gets server time. Backbone dependency handled as planned: the Gaussian head targets the shared v8/11 `Detect` codepath and ports to whichever DFL variant Table 1 selects (plan B1 conditionals cover a non-DFL winner). | Wall-clock win; head work is the §18 highest-risk item | plan D; scope §18; Laksh go-ahead 2026-07-07 |
| D12 | 2026-07-07 | Tracking default: TensorBoard (local); W&B = opt-in toggle in config | No account dependency; scope §11.2 allows either | plan C5; scope §11.2 |
| D13 | 2026-07-07 | Stack pinned (see `requirements.txt`): torch 2.12.1, ultralytics 8.4.90 (YOLO26 support), + UQ/eval libs; full lock frozen on server at Phase 1 start | Reproducibility per §11.1; versions verified against PyPI 2026-07-07 | scope §11 |
| D14 | 2026-07-07 | Table 3 evaluated **per-frame (α = 1.0, smoothing off)** as the headline protocol; temporal smoothing (α < 1) becomes a separate sequence-level ablation | Cleaner attribution — the headline fusion result shouldn't owe anything to temporal state; matches the scope's "instant-by-instant" claim | answer A4-13 (delegated); plan B5-4; scope §6.4 |
| D15 | 2026-07-07 | Compute: H100, no GPU-hour cap (A1-1) → full 6-variant × 3-seed grid at s-scale stands; stride-2 training subsampling approved (A2-6). **Update (Laksh, same day):** server tests deferred to later; epochs/batch set to standard values now instead of dry-run-pinned — `epochs: 100`, `patience: 20`, `batch: 32` — "we can tweak this later". Dry-run timing is now optional tuning, not a gate. | Budget constraint gone; standard defaults over premature optimization | answers A1-1, A2-6; Laksh follow-up 2026-07-07; plan C5 |
| D16 | 2026-07-07 | `dataset_requirement.md` added as the binding data contract (layout, yamls, 3-way split, filename ordinals, IR bit-depth note, calibration files). **Phase 1 grid must train on Pohang-only yamls** — if the current combined Pohang+MIT yamls (A1-2) mix MIT frames, Pohang-only yamls are required first. | Benchmark decision is Pohang-based per plan C4; a combined training set would silently change what Table 1 measures | answer A1-2; plan C4 |
| D17 | 2026-07-07 | **Gaussian head integration = in-place conversion, no Ultralytics fork.** A loaded `DetectionModel` gets a fresh `cv4` log-variance branch added to its live `Detect` head + a class swap (`GaussianDetect`/`GaussianDetectionModel`); trainer is a thin `DetectionTrainer` subclass (4th loss item, warm-up epoch sync callback). σ parameterized as log σ² over LTRB distances in stride units (same targets as DFL), converted to pixels at inference; σ rides through NMS as extra channels. **Gradient policy defaults (A4-11 conservatism): σ branch reads detached features, NLL sees detached μ → deterministic detector trains bit-identically to baseline by construction.** Warm-up = NLL weight 0 (σ's only gradient source) — no param-group surgery. All relaxable via the `gaussian:` config block for the Phase 2 ablation. | Survives ultralytics upgrades better than a vendored fork; pretrained weights untouched; parity guaranteed, not hoped for | plan B1; scope §6.2, §11.3 B.1; verified against pinned ultralytics 8.4.90 internals |
| D18 | 2026-07-07 | **DFL-derived uncertainty (§7.2 option (a)) emitted at inference from the same trained model** — per-coordinate std of the DFL bin distribution, appended after σ. Table 2's "DFL-derived vs explicit Gaussian" ablation therefore needs zero extra training. | The single-model ablation D1 promised, made concrete | plan B1; scope §7.2; R2 |
| D19 | 2026-07-07 | **MC-Dropout protocol pre-fixed:** one `Dropout2d` inserted before the final 1×1 conv of each head branch (cv2 + cv3, every level); p=0.15, T=10 (config `baselines.mc_dropout`); retrained from pretrained weights; its own training results.csv IS the required deterministic row (val runs dropout-off) | Dropout isn't free on YOLO — a different deterministic model, disclosed | plan B6-3; scope §9.2, R5 |
| D20 | 2026-07-07 | **One cross-pass/member matching protocol** (`uq/clustering.py`, shared by MC-Dropout and ensembles): greedy by descending conf; per other source, best-IoU unused same-class detection at IoU≥0.55 joins; cluster conf = mean(conf)·support/n_sources; σ = per-coordinate member std; **singletons get σ = box size** (max size-normalized uncertainty ≈ 1) — a disclosed convention, not NaN | Calibration metrics are sensitive to matching; fixed as method, not knob | plan B6-4 |
| D21 | 2026-07-07 | **Metric definitions pre-registered in code** (`eval/metrics.py` module docstring): D-ECE (conf-vs-precision bins @IoU≥0.5), per-edge z-score coverage + interval-ECE, per-edge Gaussian NLL, AUSE + AURC (risk = 1−IoU, FP=1), OOD AUROC; mAP = local COCO-style 101-point (no pycocotools dependency). Calibration-under-shift = same metrics on corrupted caches | Kills post-hoc metric shopping (B6-5) and makes B6-6 a cache re-read | plan B6-5/B6-6; scope §12 |
| D22 | 2026-07-07 | **Ensemble = M seed replicates trained via the Phase 1 grid runner** (resume-safe; per-member deterministic rows free in the CSV); M=5 seeds {0..4} in config; frame features for OOD taken from member 0 only (one consistent feature space) | plan B6-7 seed accounting, executed | plan B6-7; scope §9.4, R4 |
| D23 | 2026-07-07 | **Corruptions** (fog/rain/lowlight/glare/blur/noise) via albumentations, severity 1–3, deterministic per frame from (seed, index); the seed is stamped into cache meta. **Anti-leakage rule operationalized: tuning caches and final-test caches must use different `--corrupt-seed`** | plan B5-5's train/test degradation separation, enforced by convention + traceable metadata | plan B5-5; scope §5.2, §11.3 B.6 |
| D24 | 2026-07-11 | **D8 amended (Laksh):** Phase 1 grid expanded from the 6-variant s-only shortlist to the **full size ladder per family** (31 variants: v8 n/s/m/l/x, v9 t/s/m/c/e, v10 n/s/m/b/l/x, 11/12/26 n/s/m/l/x — per-family scale letters verified against ultralytics), and run at **single seed `[0]`** instead of {0,1,2}. Grid: 6×3=18 → **31×1=31 runs**. Selection rule loses its cross-seed std, so it becomes: top mAP@50–95, tie-break DFL > simplest fork > FPS; seed variance is spent later on the winner only. All 31 `.yaml` variants build clean (verified on dev ultralytics 8.4.7; server pins 8.4.90). Config-only change — grid/table/fps/smoke harness is variant-agnostic. | Wanted the full accuracy-vs-size curve per family; single seed keeps a 31-model sweep cheap for winner selection | Laksh 2026-07-11; config.yaml `benchmark.variants`/`seeds` |
| D25 | 2026-08-17 | **Backbone = `yolo26m`; main-backbone training moves to the full-resolution twin** (`D:\Datasets\Pohang_dataset_full`, VIS 2048×1080 native). Rule-compliant under D8/D24: `26m` sits inside the leading group's pooled seed sd, and since no YOLO26 variant has DFL the tie-break falls to FPS, which `26m` wins (56.4 vs 30.5 fp32 against `yolo26x`) at 2.7× fewer params. Disclose the soft spot: the 0.0033 gap to `26x` exceeds `26x`'s own sd (0.0020) while sitting inside `26m`'s (0.0050). | Owner decision; the 640 set stored VIS pre-letterboxed (real content 640×338, 47% gray), so native frames are the `imgsz`/small-object lever OQ-7 named | Laksh 2026-08-17 |
| D26 | 2026-08-17 | **Gaussian σ² head ported to end2end heads** (`uq/gaussian.py`), with three differences forced by YOLO26's shape: (i) **σ rides the one2one branch** — inference decodes from one2one and the two branches use different assignments (topk 10 vs 7/topk2 1), so σ on one2many would describe a predictor that never reaches the output; (ii) **`postprocess` overridden** to gather σ with the boxes' top-k index — stock splits `[4, nc]` exactly, so trailing σ columns would be read as class logits; (iii) **μ = raw box output and the NLL target stays unclamped** at `reg_max=1` (`bbox2dist(..., reg_max-1)` collapses every target to the constant −0.01). The **NLL is added outside the o2m/o2o schedule** (which moves one2one 0.2 → 0.9 across a run) so `nll_gain` and the warm-up keep their meaning and the detector's three terms stay bit-identical. **Known limitation:** `sigma_detach_features=False` cannot be honored on end2end heads (ultralytics detaches one2one upstream) — forced True with a warning; that ablation needs a plain-`Detect` backbone. | Required under **both** §7.2 options, so it presumes no answer there; each of the three hazards yields σ that is finite, positive and non-degenerate, i.e. passes every aggregate check while being wrong | this session; gated by `scripts/smoke_gaussian_e2e.py` |
| D27 | 2026-08-20 | **Decision layer finalized (sensor selection, not fusion):** capability-weighted WBF (`iou_thr` 0.85) with the capability prior computed **run-disjoint** (fit runs only); hard photometric veto (`p05` margin rule, `b < mu_b`) with **dilate-15 hysteresis** in capture order; **photometric soft term retired** (`bright_soft=False` — `veto_only` equals gate+veto in every measured cell); Mahalanobis D stays soft-weight-only, never a switch; σ-weighted WBF kept as implemented-with-diagnosed-null; night-superiority claim retired (CI spans zero; inverts under the p2feat IR detector). Pre-registered drop condition for the soft weight was tested same-day and **not met** — Mahalanobis is kept: its removal costs fog/night −0.0028 (CI excludes zero), the frames the dilated veto still misses, at a +0.0023 opportunity cost on glare/day (disclosed). Bonus finding: glare/day is the one cell where gated fusion beats BOTH single streams (vs VIS +0.0022 [+0.0004, +0.0042]). | The follow-up round's bootstrap discipline; §3/§4/§7 of the followup record | `docs/architecture-final-2026-08-20.md` §1–§2; `runs/eval/final_system.md` |
| D28 | 2026-08-20 | **IR full-scale recipe:** `yolo26s-p2feat`, imgsz 640, min–max 8-bit map (no CLAHE), no rect, stride 1, 100 epochs. Ship-only (nc=1) pending stage-3 per-class ship AP, rule pre-registered: adopt iff ship-AP delta > +0.003. VIS: 640 on the prepared tree, stride 2; OQ-11 (full-res/1280) excluded from the matrix. Matrix stays on `yolo26s` — `26m`/full-res becomes one follow-up arm (narrows D25, **needs Laksh sign-off**). | p2feat is the one large measured lever (+34% IR night); everything else screened null or confounded | `docs/architecture-final-2026-08-20.md` §3 |
| D29 | 2026-08-20 | **Baselines inherit every detector-side change** (p2feat, imgsz, class set, data) so Table 2 compares UQ methods, not architectures; the veto/hysteresis is a shared UQ-agnostic system component — Table 3 rows hold the fusion rule fixed and swap only the uncertainty source. | Anything else confounds the paper's remaining UQ claim | `docs/architecture-final-2026-08-20.md` §4 |
| D30 | 2026-08-20 | **GPU interpreter pinned:** `gpu_python` in config.yaml + `resolve_gpu_python()` (CUDA-probed before launch); chain scripts must never inherit `sys.executable`. | Stage 4 ran 4 epochs at a 19.8× penalty on the CPU torch build (followup §9) | `src/uqfusion/config.py`; `scripts/chain_stage4.py` |

## Answers received (Laksh, 2026-07-07) — §A questions

| Q | Answer | Consequence |
|---|---|---|
| A1-1 | H100, no GPU-hour limit | D15: full grid stands; batch default 32 pending dry-run |
| A1-2 | Combined Pohang+MIT data already in README layout | D16: Pohang-only yamls required for the Phase 1 grid — see dataset_requirement.md §2 |
| A1-3 | Colab/Kaggle not needed | — |
| A2-4 | Custom train/test split already built; wants leakage confirmation | D6-rev: `scripts/audit_split.py` is the confirmation; must PASS before training |
| A2-5 | pohang01 (night) goes **into training** | D6-rev; night test blocks requested to keep a real-night Table 3 row |
| A2-6 | Stride-subsampling approved | `benchmark.train_stride: 2`; `scripts/make_stride_subset.py` |
| A2-7 | Classes = ship, buoy. MIT annotation count/QA **still open** | Class harmonization ✓; MIT onboarding schedule still unknowable |
| A2-8 | Labels complete; calibration files **still open** | D7 (homography) blocked until answered — Phase 2, not urgent |
| A3-9 | No venue/deadline | Optional items scheduled by value, not deadline |
| A3-10 | No release planned — licensing deferred | — |
| A4-11 | σ² branch approved conditionally (must earn its keep empirically) | D1 amended; interpretation on record there |
| A4-12 | No preference → D8 default confirmed | VIS grid + IR top-2 confirmation |
| A4-13 | Claude's call on Table 3 smoothing | D14: per-frame (α=1) headline, smoothing as ablation |

Also: one-phase-at-a-time with explicit per-phase go-ahead restated (D11 overlap suspended); `dataset_requirement.md` requested and written.

## State — implemented / stubbed / untested

| Component | Status |
|---|---|
| Repo scaffold (git, layout, .gitignore) | **Implemented** (this session) |
| `config.yaml` + loader (`src/uqfusion/config.py`) | **Implemented + smoke-tested**: `python -m uqfusion.config` |
| `requirements.txt` (pinned) | **Implemented + install-verified** (clean resolve, py3.13). Note: `lightning`/`rich` pinned explicitly because torch-uncertainty 0.12.1 needs them at import but doesn't declare them |
| `scripts/smoke_env.py` (env + variant-name check) | **Implemented + green**: all imports OK; all **31** Table 1 variants (full ladder, D24) build in ultralytics 8.4.7/8.4.90 — smallest/largest per family e.g. v8 3.2→68.2M, v9 2.1→58.2M, v10 2.8→31.8M, 11 2.6→57.0M, 12 2.6→59.2M, 26 2.6→59.0M (run on server post-install too) |
| Data-list utilities (`uqfusion/data/lists.py`: yaml/txt/dir resolution, run keys, frame ordinals) | **Implemented + exercised by smoke** |
| Split audit (`uqfusion/data/audit.py` + `scripts/audit_split.py`) | **Implemented**; logic exercised on synthetic lists only — real verdict happens on the server against Laksh's split |
| Stride subsetting (`uqfusion/data/subset.py` + `scripts/make_stride_subset.py`) | **Implemented**; untested against real Pohang lists (needs server data) |
| Benchmark grid (`uqfusion/bench/grid.py` + `scripts/run_benchmark.py`, resume-safe CSV) | **Implemented + CPU smoke-tested end-to-end** (`scripts/smoke_benchmark.py` → SMOKE OK) |
| FPS harness (`uqfusion/bench/fps.py` + `scripts/measure_fps.py`) | **Implemented + smoke-tested** (fp16 path untestable on CPU — first real test on server) |
| Table 1 generator (`uqfusion/bench/table.py` + `scripts/make_table1.py`) | **Implemented + smoke-tested** (params/GFLOPs populate; env stamp embedded) |
| Val-split carving tool (train/test → train/val/test) | **Not built** — only needed if Laksh's split lacks val (open question OQ-1) |
| Gaussian σ² head + β-NLL/warm-up (`uq/gaussian.py`, `uq/train_gaussian.py`) — **plain `Detect` / DFL path** (v8/v9/11/12) | **Implemented + §18-2 gate GREEN** (`scripts/smoke_gaussian.py`): warm-up engages (nll=0 epochs 1–2), σ non-degenerate (CV 0.42), **σ tracks injected noise** (noisy-cue u 0.0639 > clean-cue 0.0538), detection unharmed (toy mAP50 0.97). Re-verified unregressed after the D26 port (CV 0.364, direction PASS 0.0640 > 0.0539) |
| Gaussian σ² head — **end2end path** (YOLO26, `reg_max=1`; D26) | **Implemented + §18-2 gate GREEN** (`scripts/smoke_gaussian_e2e.py`): σ on one2one only, top-k gather keeps σ with its box for all rows, **detector parity exact (max \|Δ\| 0.00e+00)**, warm-up logs nll=0, σ mean 7.57 px CV 0.291, `dfl_sigma_ltrb` correctly absent |
| End2end §18-2 gate (`scripts/smoke_gaussian_e2e.py`) | **Written + GREEN.** Five structural checks run in seconds with no training — they exist because each end2end hazard produces σ that is finite, positive and non-degenerate, so the aggregate checks in `smoke_gaussian.py` cannot see them |
| DFL-derived uncertainty extractor (§7.2 option (a), D18) | **Implemented + smoke-verified** — emitted from the same pass; toy stats: 9.39px, CV 0.81 vs trained σ 4.47px, CV 0.42 |
| UQ inference (`uq/infer.py` UQPredictor: σ through NMS + pooled features + safe-globals ckpt loading) | **Implemented + smoke-tested** |
| Mahalanobis OOD scorer (`uq/mahalanobis.py`) | **Implemented + smoke-tested** — toy separation: clean d̄ 33.9 vs degraded 59.3, 100% beyond clean 95th pct |
| `compute_reliability()` + constants fitting (`uq/reliability.py`, §6.4 verbatim incl. empty-frame fallback, R_sys) | **Implemented + smoke-tested** — gate favored clean stream 16/16 frames (R̄ 0.81 vs 0.16) |
| Reliability-weighted WBF fusion + homography transform (`uq/fusion.py`) | **Implemented + smoke-tested with identity H** — real IR→VIS homography blocked on calibration files (OQ-5); per-fused-box σ propagation deferred to Phase 3 |
| Prediction caches (`eval/cache.py` + `scripts/build_cache.py`, gaussian/mc/ensemble sources, optional corruption, meta-stamped) | **Implemented + smoke-tested** (8 caches round-trip) + CLI verified |
| MC-Dropout (`uq/mc_dropout.py` + `scripts/train_mc_dropout.py`; D19 protocol) | **Implemented + smoke-tested**: 6 dropout layers armed, cluster σ CV 1.62, toy deterministic mAP50-95 0.60 |
| Deep Ensemble (`uq/ensemble.py` + `scripts/train_ensemble.py`; D22, trains via grid runner) | **Implemented + smoke-tested** (M=2 toy): member-disagreement σ CV 0.96 |
| Shared clustering protocol (`uq/clustering.py`; D20) | **Implemented + smoke-tested** via both baselines |
| Corruption generation (`eval/corruptions.py`, 6 conditions, albumentations 2.0.8; D23) | **Implemented + smoke-tested** (all 6 valid + changed) |
| Pre-registered metrics (`eval/metrics.py`: D-ECE, interval-ECE/coverage, NLL, AUSE, AURC, OOD AUROC, local COCO mAP; D21) + `scripts/evaluate_uq.py` | **Implemented + smoke-tested** — finite for all 3 sources incl. the DFL §7.2 row; observed toy-scale MC/ens NLL blow-up (4003/627 vs Gaussian 2.8) = the known agreeing-passes-overconfidence effect, informative not a bug |
| Learned gate (`eval/learned_gate.py`; D4/B4 design) | **Implemented + smoke-tested**: favors clean stream 100% (mean w_vis 0.101) |
| Fusion-system eval + gate ablation (`eval/fusion_eval.py` + `scripts/ablate_gate.py`) | **Implemented + smoke-tested**: gated 0.739 vs blind-VIS 0.000 vs IR 0.778 mAP50-95; 6-row ablation sweep |
| `HOW_TO_RUN.md` (per-phase runbook) | **Written; all documented CLIs verified on this machine** |
| Phase 4 (Table 3 runner, real VIS↔IR pairing, MIT onboarding, DETR row) | **Not started** — awaiting phase gate |
| Gaussian σ² head + β-NLL (scope §6.2) | Not started (Phase 2; CPU-side may overlap Phase 1) |
| `compute_reliability()` (scope §6.4) | Not started (Phase 2) |
| IR→VIS homography + WBF fusion | Not started (Phase 2; blocked on A2-8 for calibration files) |
| Mahalanobis OOD scorer (scope §6.3) | Not started (Phase 2) |
| MC-Dropout / Deep Ensembles / calibration suite | Not started (Phase 3) |
| MIT dataset onboarding | Not started (Phase 2+, parallel) |

## Open questions — awaiting Laksh

| # | Question | Blocks |
|---|---|---|
| OQ-1 | ~~Does the custom split include a `val` set?~~ **RESOLVED (2026-07-10):** delivered split had train/val/test but was frame-interleaved (leaking). Re-split into contiguous per-run ordinal blocks by `scripts/prepare_pohang.py`; audit now PASSES. | ~~Grid launch~~ done |
| OQ-2 | ~~Are the Phase 1 yamls Pohang-only?~~ **RESOLVED (2026-07-10):** yes, Pohang-only (pohang00–04, no MIT). | ~~Grid launch~~ done |
| OQ-3 | ~~IR bit depth/normalization?~~ **RESOLVED (2026-07-10, Laksh):** IR was 8-bit via **per-frame min–max**; documented in `Pohang_dataset/IR_PREPROCESSING.md`. IR also letterboxed 640×512→640×640. | Crossover realism + Mahalanobis note on record |
| OQ-7 | ~~Letterboxed or stretched?~~ **ANSWERED: letterbox (Laksh 2026-07-08).** Still open: confirm full-res originals stay archived (imgsz is the small-object lever). | Buoy-recall headroom only |
| OQ-8 | **MIT fusion camera pair:** OK to fix left-VIS ↔ left-IR as the canonical fusion pair (present in both rig configs; one homography)? Other cameras still pool into detection training. Also: does the MIT release ship any calibration files? (Route A vs Route B in docs/calibration_and_registration.md) | MIT pairs.csv + homography plan (Phase 4 / MIT onboarding) |
| OQ-4 | MIT annotation count + QA status (carried from A2-7) | MIT onboarding schedule (Phase 2+) |
| OQ-10 | **§7.2 resolution.** YOLO26 has no DFL (`reg_max=1`), so option (a) is *undefined* on the selected backbone — D18's free ablation row cannot exist. Option A = also train `yolo12m`+cv4 (compute-matched control: 20.2 M/68.1 GFLOPs/56.1 FPS vs 21.9/75.4/56.4) and keep the explicit-vs-DFL comparison *within that model*; Option B = drop option (a) and rest on the structural fact plus the NMS-free-semantics argument (§8/R17). Cost of A: ~40–45 h GPU at imgsz 1280, **zero new code** — both arms are implemented and gated. Note the risk either way: the free DFL signal may calibrate *better* than cv4, which bears on D1/A4-11's "earn its keep". | Table 2 row count; nothing else |
| OQ-11 | **`imgsz` for the full-res runs.** 640→1280 is exactly 2× linear content resolution (the 640 set stored VIS pre-letterboxed at 640×338) but 4× the pixels: ~80–90 min/epoch, ~40–45 h/seed, and it compounds across ~16 Phase 2–3 models. Recommend measuring first: `26m` seed 0 at 640 (existing row) vs 1024 vs 1280, scored on **per-class** AP so the buoy column answers OQ-7. Two traps on record: `rect=True` silently forces `mosaic=0`, and the 306 GB of native PNG should be pre-resized once to the chosen `imgsz` (no padding → labels copy verbatim) rather than shipped. | Phase 2 launch; the resized twin; the stride yaml regeneration |
| OQ-12 | **Where full-res VIS trains, and the ensemble budget.** `26m` peaked 4.3 GB at 640/batch 8, so 1280 ≈ 17 GB on the laptop's 12.3 GB card — and WDDM pages instead of OOMing (measured: 17× slower, silently). DGX MIG slice is the only viable host. M=5 ensemble at full-res ≈ 9 GPU-days for VIS alone; keep M=5 or drop to M=3 and disclose? | Phase 3 schedule |
| OQ-5 | ~~Pohang calibration files?~~ **PARTIALLY RESOLVED (2026-07-10):** per-run intrinsics+extrinsics present at `Pohang_dataset/meta/pohangNN/calibration/` (keys incl. `infrared`, `stereo_left/right`). Homography D7 build still Phase 2; note calibration is **per-run** (one H per run). | Homography D7 — Phase 2 |
| OQ-9 | **Data placement/paths for the server.** yamls now carry an absolute dev-machine `path:` (`A:/Uncertain/Pohang_dataset/{visible,infrared}`); update per machine. Also `config.yaml` expects `{data_root}/pohang/data_vis.yaml` but the folder is `Pohang_dataset/` with modality subdirs — reconcile placement on the server (put dataset at `{data_root}/pohang/` or point config at Pohang_dataset). | Grid launch on server |
| OQ-6 | ~~Dry-run timing report~~ **Demoted per Laksh:** epochs/batch set to standard values (100/32, patience 20); dry-run timing is optional tuning whenever the server runs happen | Nothing — tweak-later |
| OQ-13 | **(2026-09-04: one hypothesis narrower, and instrumentation now exists.)** `A:` is a local NTFS fixed disk, not a synced volume, and VSS + File History are both Stopped/Manual -- no scheduled snapshot service could have rolled the directory back on its own (a one-off manual revert is not excluded; confirming that needs admin). `scripts/label_hash_ledger.py` now keeps an append-only timeline (hash + file count + box count + newest mtime, exit 1 on drift) so a recurrence has a bounded window instead of a guess. It also found **8,237 label files belonging to no split list** -- invisible to every existing gate, and exactly where a by-directory rewrite would hide. Original question: **What rewrote 7,591 `pohang01` train labels at 21:19 on 2026-09-03?** No project script writes labels (`verify_dataset_state.py` and `build_cache.py` were both audited for write paths and have none), the shell was idle, and no Python process was live. Owner states nothing else touches the directory. It has happened once; if it recurs mid-training it corrupts a run silently, and only the launch-time hash gate would catch it — nothing checks *during* a run. | Trust in every long run; consider a post-run hash assertion |
| OQ-14 | ~~**Where does this repo actually live?**~~ **RESOLVED 2026-09-03:** `github.com/Laksh-saroha/uqfusion`, public, both branches pushed. The server is still a manual copy rather than a clone -- making it a real clone needs a per-machine `config.yaml` story first (OQ-9). Original: There is no remote. Both pre-registrations, the recovery, and every September commit exist only on the laptop. Decide: private GitHub, or a bare repo on backed-up storage. Until then the server cannot be brought up to date by pulling, and a disk failure loses the record. | Everything; blocks server sync |

## Next actions

**The canonical runbook is now [`HOW_TO_RUN.md`](HOW_TO_RUN.md)** — per-phase commands for the server, verified locally.

**Laksh, on the H100 server, whenever server time happens:**
1. Setup + full smoke suite (HOW_TO_RUN §0–§1; expect CUDA True and all five OK lines).
2. **Phase 1:** audit (must PASS) → stride → full grid → FPS → Table 1 → IR top-2 confirm (HOW_TO_RUN §2). Send back `table1.md`, `table1_ir.md`, audit report.
3. **Phase 2:** Gaussian training on the winner + the §12.1 parity row (HOW_TO_RUN §3).
4. **Phase 3:** baselines → caches → `evaluate_uq` (Table 2) → `ablate_gate` (HOW_TO_RUN §4).
5. Answer the OQ table above (OQ-1/2/3 gate real-data training; OQ-5 calibration files gate the homography).

**Immediate, 2026-09-09 — what the U2 verdict did and did not settle:**

*Resolved, and struck from this list:* the VIS ensemble arm was built (all 5 seeds, local,
cold-started) so Stage B is closed; the UQ slice re-ran on night-capable arms under its own
pre-registration ([`docs/prereg-uq-day-night-slice-u2-stageb.md`](docs/prereg-uq-day-night-slice-u2-stageb.md),
`4a4a84b`), and it returned **VIS SUSPECT / IR SUSPECT**.

1. **Do NOT spend the re-baseline budget on the strength of U2.** Both modalities landed in the
   same band, which is the pre-registered reading that the distortion is **not label-driven**.
   IR labels were never filtered. Night is intrinsically harder to calibrate on for both
   sensors, and **no amount of retraining repairs that.** U2 §7 fixes the consequence in
   advance: day-only becomes the primary reporting basis, pooled is retained as secondary with
   its 46.2% night share in the caption. No retrain, no detector swap.
2. **Give `nll` an explicit NaN policy** (S, CPU-only). `sep 0.0000` on both modalities is an
   artifact: Python's `max`/`min` silently skip NaN, so `max(dv)-min(dv)` over
   `[3.9057, nan, nan]` collapses to zero. NO-SIGNAL is the right *outcome* with two of three
   arms unmeasurable, but the table reads as an exact tie, and with only ONE arm NaN the
   statistic would be computed over a subset while the prereg specifies `max_a` over all arms.
   No verdict moves; it is a live trap for the next run.
3. **[`docs/rebaseline-proposal-2026-09-04.md`](docs/rebaseline-proposal-2026-09-04.md) §4 is now
   decidable on its own terms** — `gauss_vis_seed0_nightfull` is the drop-in for the
   `gauss_vis_seed0_ft` wired into `preset=crossmodal26m`, at ~2.5 h of caching and refitting.
   One caveat on record: the new run is **single-stage**, so it is not a bit-for-bit replacement
   for the two-stage `_ft` checkpoint. Needs its own registration; U2 licenses none of it.
4. **The architecture review backlog**
   ([`docs/TODO-2026-09-09-architecture-review.md`](docs/TODO-2026-09-09-architecture-review.md))
   argues workstream A precedes everything. **It did not gate U2, checked rather than assumed:**
   none of the five decision metrics routes through the AP path — only the reported,
   non-decision `map50_95` column does. It still gates every mAP-ranked table.
5. **Server: nothing to do but watch.** `runs/queue_vis_benchmark_ep25/status.xlsx`;
   its HEALTH line reads STALLED if the queue stops writing `state.json`. The 31-run redo pass
   is in [`docs/handoff-2026-09-06-redo31.md`](docs/handoff-2026-09-06-redo31.md).

**Then (me):** selection memo + §7.2 resolution from Table 1 → port Gaussian conversion if the winner isn't v8/11-family → analyze Table 2/ablation outputs → **phase gate: Laksh's go-ahead for Phase 4** (Table 3 runner with real VIS↔IR pairing + homography, both-degraded row + R_sys histogram, MIT onboarding, DETR row decision, multi-seed publication runs, landscape re-check per plan C1).

## Run log

| Date | Phase | Run | Config / commit | Result | Notes |
|---|---|---|---|---|---|
| 2026-09-09 | — | **cap_ir gap chased; R-A5 rebuilt on the right artifact** — `runs/eval/change_impact_v4.md`, ledger §6 | `6e6bbac` | **CORRECTION: there is no drift and nothing fails to regenerate.** Every value is deterministic and bit-reproducible; the earlier '1.83% residual' was my arithmetic error (I read the recorded field as pre-scale when it is the post-scale `ctx.cap_ir`). **What is actually broken is the MANIFEST:** three artifacts all record `"preset": "crossmodal"` and disagree on `cap_ir` — 12:36 `0.0092465` (no IR NMS, no cap scale), 17:09 `0.0094158` (NMS 0.70), 19:03 `0.0023539` (NMS 0.70 ÷ 4). `preset="crossmodal"` gained `ir_nms=0.70` and `cap_ir_scale=4.0` in `b3d8371` on that same day and the config block records **neither** | Verified not argued: recomputing from `gauss_ir_paired_clean.pkl` returns `0.009246512091269591` bit-for-bit; NMS 0.70 then ÷4 returns `0.0023539426113108287` bit-for-bit. `cap_vis` identical throughout, and invariant to `SORT_KIND`, so neither R-A1's stable sort nor R-B2 is involved. The 12:36 log betrays its vintage only by omission — `IR 0.0092 (ratio 36.2x)` with no 'IR scaled 1/…' clause. **Consequence for R-A5:** the change-impact table had been built on the 12:36 artifact and labelled it the shipped preset — **two revisions behind**. Rebuilt on `final_system_crossmodal_v2.json`, and under it **all 7 gated-vs-visible_only cells SURVIVE**, including glare (+0.0054 macro / +0.0086 ship) and clean/ship (+0.0052) — both previously reported as casualties. Overall shape unchanged: 20 of 74 indeterminate, `no_veto` still stands on 18 of 26. **The fix this points at is R-E1:** a config block must record every value that changes the system, not a preset NAME, which is a moving target |
| 2026-09-09 | — | **R-B2: exposure labelled + final-score guard** — `DEVELOPMENT_RUNS`, `role=` on `load_context`, `assert_final_scorable()`, `docs/exposure-ledger-2026-09-09.md` | `38a128c` | **This repository contains no untouched test set, and that is now stated rather than reconstructible.** `sel("fit") == sel("day")` is **True** in code — `FIT_RUNS` excludes only `pohang01`, which is entirely night, so every constant tuned on 'fit-run day frames' was reported on the frames it was tuned on. `TEST_RUNS` was declared after exposure and has **rejected a candidate** (support IoU 0.55, 2026-09-01 log), which is participation in selection. `role="final"` now makes `capability_sel` required and refuses any selector spanning the scoring frames; `assert_final_scorable()` intersects fit-frames with score-frames and refuses on overlap | **No number moves:** `role="develop"` is the default, all 63 call sites unaffected, and bit-identity verified by stashing `ctx.py` and re-running (`cap_vis` AND `cap_ir` identical). Making `capability_sel` required unconditionally would have broken 56 of 63 sites and silently rewritten every recorded number, so it is required only where it can do harm. **Still open — the expensive half:** nested LORO with every fitted component inside the fold. The guard can refuse a contaminated final score; **there is still no uncontaminated data to run one on** (ledger §5 prices the options). **Found while doing this, logged not diagnosed (R-E1/F14):** `final_system_crossmodal.json` records `cap_ir` 0.0092465 but the current tree reproduces 0.0023539 from the same preset while `cap_vis` is bit-identical — ratio 3.93 vs a `cap_ir_scale` of 4.0, and a residual **1.83%** after that. **A shipped result that does not regenerate is its own finding** |
| 2026-09-09 | — | **R-A5 first pass** — change-impact table, `scripts/change_impact_table.py`, report `runs/eval/change_impact_v3.md` | `f8c6b7e` | **16 of 53 findings defended by a zero-excluding interval no longer are.** Not refuted — the effects may be real — but the interval used to defend them no longer supports them. Per claim: `no_veto` ("the veto is load-bearing") **stands**, 12 of 18 cells, down to |delta| 0.0037; `gated vs visible_only` 10 of 14; `with_maha`/`no_maha`/`cap_only` lose their clean/day cells (razor-thin already — one recorded CI ran to −7.15e-7); `photometric_veto` and the veil repair hold entirely | **The row to read carefully:** the **shipped** crossmodal preset KEEPS glare (+0.0045 macro, +0.0068 ship); the older 2026-08-20 D27 system LOSES it (+0.0022 → [−0.0017, +0.0064]). The recorded headline *'glare/day beats both single streams (+0.0022 [+0.0004, +0.0042])'* is the OLD system's number and is now indeterminate. **Also newly indeterminate in both systems:** gated vs visible_only clean/ship — which is the exact instance quoted inside `matching.map50_95`'s own docstring (*'ship AP +0.0031 CI [+0.0015, +0.0052], not spanning zero'*) to argue macro dilution hides a real ship gain; the structural argument survives, the cited instance does not. **Scope, stated not implied:** this RE-ADJUDICATES recorded intervals, it does not RE-SCORE — the 1.95× factor was measured on VIS UQ-arm deltas and transported to fusion cells, which is an assumption. A full re-score via `blockboot` is mechanical now and is the next step for any row in doubt. Three of the five named families are excluded rather than fudged (reprice = sign rule; soft-NMS = draw noise not frame noise; crossmodal tuning = selection without intervals, R-B2's territory) |
| 2026-09-09 | — | **R-A3 closed** — paired moving-block bootstrap, `src/uqfusion/eval/blockboot.py`, sweep in `scripts/interval_block_sensitivity.py` | `e2ba483` | **This project's intervals are ~1.9× too narrow, and that is a LOWER BOUND.** se/se(L=1) = 1.61× at a one-second block (L=10), 1.95× (worst 1.99×) at L=20, the largest defensible block — and the curve is still rising there. **Unlike R-A1/R-A2 this one moves things:** the 0.0014–0.0031 noise floor was itself computed with the iid bootstrap, so it is understated by the same factor, and every margin defended as 'just outside the CI' needs re-reading. Report: `runs/eval/interval_block_sensitivity_v3.md` | **Two self-corrections the run forced, both kept in the report.** (1) v1 quoted the L=200 ratio as the headline — an artefact: `pohang03` has 117 frames, so at L=200 it admits exactly ONE block start and the variance COLLAPSES. Blocks are now validity-bounded at shortest-run/5 = 23. (2) v2 then said the ratio 'falls at L=50' — it does not, L=50 and L=100 are still rising (2.90×, 3.23×); they are just past the bound and unreliable in either direction. The script now computes where the curve actually turns over. **Not estimable at any block length:** night is entirely `pohang01`, so the between-night-run component does not exist in this data — every night interval is conditional on that one recording. `describe()` returns the resampling unit and the uncovered randomness with every interval (the acceptance criterion); `sign_flip_fraction` replaces `p_sign_flip`, which is **not a p-value** and survives as a deprecated alias only |
| 2026-09-09 | — | **R-A1 harness + F03 measured** — `src/uqfusion/eval/cocoparity.py`, `scripts/smoke_cocoparity.py`, `scripts/ap_convention_parity.py` | `a3182ba` | **The convention gap is real but 5× below the noise floor in deltas.** Absolute AP: local reads systematically LOW, −0.00039 (VIS sigma, all) to −6e-7 (IR). Deltas: the offset cancels because both arms share the convention — **worst disagreement 0.00028501 vs a 0.0014–0.0031 paired 2σ floor.** F03 cannot flip a decision whose margin clears the floor. It does **not** rescue decisions below it: soft-NMS was rejected at −1.03e-5, which is 20× smaller than this disagreement. Report: `runs/eval/ap_convention_parity.md` | Both review examples reproduced to the digit before building (0.83168317/0.83498350 and 0.74752475/0.75247525). `TASK_CONFIG` pins every confounding knob; `maxDets` comes from the data, NOT COCO's 100, which would truncate a `conf 0.001` cache and disguise a detection cap as an interpolation gap. All 8 acceptance cases pass — **the detection-cap case was VACUOUS on first write** (capping changed no score, so it tested only metadata) and was rebuilt with TPs ranked below 100 high-conf FPs so capping drops 0.1667 → 0.0; same failure mode as R-A2's guard, worth watching for. `matching.py`'s three argsorts pinned stable so `TASK_CONFIG`'s claim is true of the local path too. Installed `pycocotools==2.0.11` (prebuilt wheel) into system Python and declared it in `requirements.txt`. **Unexplained, reported as observed:** the gap is far larger on day than night, consistently across arms. **Open:** the (a) replace / (b) rename+companion call is the owner's; recommendation is (b) |
| 2026-09-09 | — | **R-A2 closed** — missing-class policy + stable sort in `src/uqfusion/eval/apmetrics.py`, acceptance tests in `scripts/smoke_apmetrics.py` | `a4cf208` | **Both defects reproduced before being fixed.** Missing class: same resample, `ap_from_parts` 1.0000 vs `ap_weighted` 0.5000 — the review's own counterexample, on our code. Tie order: `np.argsort` defaults to an UNSTABLE introsort, so tied confidences ordered arbitrarily and AP moved with them. Now `MISSING_CLASS_POLICY="drop"` (COCO: no GT ⇒ undefined, not zero) and `SORT_KIND="stable"`, both named module constants. `_score` returned 0.0 for an undefined per-class request → NaN, and `bootstrap_delta` now drops those draws and reports `n_undefined`/`n_effective` | **Nothing published moves, and that was measured rather than assumed.** Buoy GT is in 153 of 2,232 paired-val frames ⇒ P(a draw deletes all of them) = `((n-k)/n)^n` = **1.5e-69**; the missing-class bug bites only on SMALL subsets (per-cell tables, per-run slices, LORO folds). The tie effect is 0.0067 mAP on a tie-dense synthetic fixture but **2.7e-7 on a real cache**, because 99.9% of 31,110 real conf values are unique — so the mechanism is real and unbounded in principle but has not been biting. Both docstrings carry both numbers; the synthetic figure alone would imply published results are at risk, and they are not. **This does not weaken the case for R-A1/R-A5** — the AP *interpolation convention* (F03) is a different defect, untouched here, and is the one with a plausibly material effect. Acceptance: smoke sections E (200 sparse-class draws, guarded against vacuity — it fired at 9/200 first try, so the fixture was made sparser rather than the threshold lowered) and F (tie determinism) |
| 2026-09-09 | 3 | **`nll` NaN policy fixed and U2 re-scored** — `arm_range` / `finite_range` in `scripts/slice_uq_day_night.py`, report `runs/eval/uq_day_night_slice_u2_nanpolicy.md` | this session | **No verdict moves: VIS SUSPECT, IR SUSPECT, IR as-registered CONTAMINATED, all unchanged.** Only the `nll` row changed, and only from a fabricated `0.0000` to an honest undefined: `sep`, `spread`, `se_sep`, `se_spread` and `floor` all `0.0 -> NaN` on both modalities. The four measured metrics are bit-identical. `nll` was NO-SIGNAL in **all three** prior runs of this slice, so nothing published needs re-scoring | **The defect was worse than a cosmetic zero: the statistic depended on arm insertion order.** Python's `max`/`min` keep their running candidate when a comparison against NaN returns False, so the same three arm values give `max([3.9,nan,1.2])=3.9` but `max([nan,3.9,1.2])=nan` -- a range of 2.7, nan or 0.0 purely from the order arms appear in `ARMS`. Verified fixed over all 6 permutations: one answer, NaN. **Policy:** rule 5 quantifies over ALL arms, so an arm with no value makes the range undefined -> NaN -> fails rule 7's floor -> NO-SIGNAL, which is what NO-SIGNAL already means. The finite-subset range is still computed and printed as a **disclosed secondary that enters no band**, with the arms that have no value named. Same guard applied to `floor` (`max(nan, x)` had the identical order-dependence), to `order()` (non-finite arms sort last as a block, deterministically), and to the bootstrap `se` (explicit undefined instead of an all-NaN `nanstd` warning). Found because `nll sep 0.0000` read as an exact tie between arms when two of three arms have no NLL at all |
| 2026-09-09 | 3 | **U2 — day/night UQ slice at Stage B**, `scripts/slice_uq_day_night.py`, 3 night-capable VIS arms + the unchanged IR control, 3 new caches in `runs/cache_uqslice/`, 2,000 paired bootstrap draws | prereg `4a4a84b`, code `0c88ae7` | **VIS SUSPECT, IR SUSPECT** (IR also **CONTAMINATED as registered**, rule 9 unfloored, on one unresolvable `ause` swap). **Same band on both modalities**, which is the pre-registered reading that the distortion is **not label-driven**: IR labels were never filtered, so night is intrinsically harder to calibrate on for both sensors and **retraining repairs nothing**. VIS driven by `ause` r=0.5982; `aurc` 0.3119 and `d_ece` 0.2992 sit just above the 0.25 CLEAN boundary and are weak on their own; `interval_ece` CLEAN; no VIS ordering flips. Consequence per U1 §7: day-only becomes the primary reporting basis, pooled retained as secondary with its 46.2% night share disclosed. **No retrain, no detector swap.** Report: `runs/eval/uq_day_night_slice_u2.md` | **The first non-hollow VIS measurement of this slice.** The pre-declared §4 diagnostic (not a decision input) confirms the retrain worked: night detections per 1,032 frames went 2 -> 7,694 (sigma-head), 28 -> 11,567 (MC), and the ensemble arm exists for the first time at 16,700. Both prior runs were decided partly by arms emitting almost nothing at night. **A reporting defect the run exposed:** `nll` shows `sep 0.0000` on both modalities and that is an artifact, not a measurement — MC and ensemble NLL are NaN (the 2026-09-04 sigma repair; an M=5 sample std is exactly 0 when members agree) and Python's builtin `max`/`min` silently skip NaN, collapsing `max(dv)-min(dv)` over `[3.9057, nan, nan]` to 0. NO-SIGNAL is the right outcome with two of three arms unmeasurable, but the table reads as an exact tie, and with only ONE arm NaN `sep` would be computed over a subset while the prereg specifies `max_a` over all arms. No verdict moves; it needs an explicit NaN policy |
| 2026-09-09 | 3 | **VIS UQ retrain complete** — `runs/queue_vis_uq_retrain`, 7 runs sequential, cold starts on the restored labels (`b92739202127`), `yolo26m` / imgsz 640 / batch 16 / epochs 100 / patience 20 | queue `runs/queue_vis_uq_retrain/queue.json` | All 7 **done**, 2026-09-04 16:56 -> 2026-09-09 21:41 IST. `mc_vis_nightfull` 97 ep, best ep 76, **0.25858**; `gauss_vis_seed0_nightfull` 37 ep, best ep 16, **0.26983**; `ens_vis_nightfull_seed{0..4}` 35–61 ep, **0.27894 / 0.26982 / 0.26808 / 0.26607 / 0.27109** | The `_ft` continuation stage was queued and then **reversed** during construction — all three VIS arms are single-stage cold starts, which makes the VIS side internally symmetric but the VIS-vs-IR comparison less so (IR control arms are all two-stage `_ft`). Disclosed in the U2 prereg §5 rather than left to be discovered. `runs/ensemble/ensemble_members.csv` does **not** need updating — it is written only by `smoke_phase3.py` and its toy `yolov8n` rows are a smoke artifact; the real per-member registry is `runs/ensemble/csv/`, where all five new members already are |
| 2026-09-04 | 3 | **Night veto V1** — `scripts/eval_night_veto.py`, cold-start `gauss_vis_nightfull`, 4 paired draws, 22 new VIS caches in `runs/cache_nv_*` (3,854 s) | prereg `5761e9f`, code `6552d68` | **INCONCLUSIVE** (band as registered: ADOPT, capped by guard 2). Clean-night **+0.1785** (0.0850 -> 0.2635) against floor 0.0054. **All 5 VIS-healthy cells ADOPT (+0.1785..+0.2255); all 5 VIS-degraded cells REJECT (-0.0102..-0.0737).** Guard 2 fired on `lowlight`/IR-glare (-0.0358) and `blur_s3`/IR-glare (-0.0159). Secondary arm `gauss_vis_nightrestore` (reported, not decision): **+0.1962**, same verdict, same two cells | Negative control passed before launch: on the shipped checkpoint the OFF arm moves night by nothing (0.0850 both ways) and day is bit-identical at 0.328562. **Rule 5 was mis-specified and was reported as such rather than silently reinterpreted** — it voids on ANY day movement, which is only sound where the veto never fires on day, and it fires on 0.8% in one cell. Partner rate at `iou_thr` 0.85 on the clean night cell is **0.143%**, so the gain is union recall and not consensus |
| 2026-09-04 | 3 | **Night veto V2** — Stage 0 screening then Stage 1 gate, `scripts/stage0_night_veto_v2.py` / `scripts/stage1_night_veto_v2.py` | prereg `c9a215c`, code `f3a24e9` | Stage 0 **PROCEED with `q_refit`** (AUROC 0.9921 vs a bar of 0.90; `sigma_frame` 0.9446, `conf_mean` 0.9263, `n_det` 0.4317 FAIL). Stage 1 **INCONCLUSIVE**: clause (a) PASS at +0.1785, clause (b) **FAIL on 5 of 11** night cells, day guard PASS | **The obvious instrument was disqualified in the registration, before the fact:** `vis_health` as shipped flags 100% of night frames unhealthy in every condition including clean, so "night AND unhealthy" is arithmetically "night". **`n_det` fails as an anti-correlated discriminator** (fog: 35.6 detections at mean conf 0.0107 vs clean 8.1 at 0.2706) and the sign was NOT flipped post hoc. V2 failed because `night_weak_fallback` — deliberately out of scope — still vetoes a HEALTHY night VIS at 46-100% through `concentrated`, a VIS texture statistic IR corruption cannot move |
| 2026-09-04 | 3 | **Night veto V3** — `scripts/v3_night_veto.py`, `veto_health_mode="sole_authority"` | prereg `0890a1a`, code `78df4fc`, record `ccf76d2` | **VOID, and the axis is CLOSED.** Clause (a) +0.1785 PASS; clause (b) **0 breaches** (V2 had 5) PASS; clause (c) 0 clean-VIS cells with a day veto PASS — then the day guard tripped on `lowlight`/IR-glare, day veto **0.8% -> 19.9%**, day delta 0.004669 vs floor 0.00200 | Night behaviour was exactly right: veto 100% -> 0% on all five clean-VIS night cells, 96-100% on the degraded ones. **The day break is the rule working as written, not a leak** — V3's trigger contains `ir_night_raw AND ~ir_ok`, which is not night-confined by construction. V3's own VOID wording conflated "leak" with "any day movement"; **that imprecision is recorded rather than exploited**, and changes nothing, since V3 is not an ADOPT under either reading |
| 2026-09-04 | 3 | **NLL repaired; no floor adopted** — `scripts/probe_nll_floor.py`, `runs/eval/nll_floor.md` | `bb72bae` | 13 of 34,776 VIS MC edges (0.037%) carry **100.0000%** of a 1.891e16 NLL; IR ensemble 0.396% of edges carry 100.0000% of 1.082e13. `gaussian_nll` now returns **NaN** when any sigma is non-positive; sigma-head arms unchanged at 5.987 / 3.873 | **No floor is neutral** — 53% of VIS sigma-head edges sit below 0.5 px, so a 0.5 px floor would rewrite the sigma-head's own NLL from 5.987 to 4.336. The deeper finding a floor does not fix: dropping the exact-zero edges still leaves VIS MC at 251 and IR ensemble at 3.3e4 against 5.99. An M-member sample std is not a calibrated sigma at small M, and repairing that is an estimator change needing its own pre-registration |
| 2026-09-04 | 1 (data) | **Label ledger seeded** — `scripts/label_hash_ledger.py`, `runs/label_hash_ledger.csv` | `a3da04d`, `86b9392` | `train` **`8ed69b5974ed`** (96,275 files, 711,444 boxes) and `tree` **`b92739202127`** (127,309 files, 962,960 boxes) both match the restored tree; newest label mtime 2026-09-03T15:58:13Z | **The seeding run caught a false equivalence in this file's own docstring**, which had claimed the `all` scope reproduces what `restore_night_perbox.py` prints. It does not — different algorithm and different file set — and leaving it would have manufactured a false DRIFT on the next run. A `tree` scope now reproduces that digest exactly. It also found **8,237 label files in no split list**, invisible to every existing gate |
| 2026-09-03 | 3 | **UQ calibration table sliced day/night** — `scripts/slice_uq_day_night.py`, 5 new caches in `runs/cache_uqslice/`, 2,000 paired bootstrap draws | prereg `a4f9364`, result `16c92ad` | **VIS CLEAN, IR SUSPECT** (as registered: CONTAMINATED). **VIS is clean for a reason opposite to the one registered:** the VIS arms emit **2 detections across 1,032 night frames** at conf 0.001, so pooled ≡ day to four decimals. The table is 46.2% night *by frames* but 0.01% *by detections*, and calibration metrics are detection-weighted — there was never a block of manufactured errors to contaminate AUSE/AURC. **No retrain is forced.** VIS CLEAN is provisional: the prereg says Stage A cannot return CLEAN, and the VIS ensemble arm is still on the server. Report: `runs/eval/uq_day_night_slice.md` | **The prereg's IR branch was wrong and the generator printed it:** it said different bands ⇒ "distortion tracks the labels", but IR labels were *never* filtered, so IR moving at night cannot be contamination — IR genuinely sees at night (night mAP 0.1538 vs day 0.0707). Fixed to three branches. **Rule 9 amended and the amendment declared in the report:** as registered ANY ordering change forced CONTAMINATED, and it fired on an AUSE swap of 0.0014 against se 0.0017 — the sign-test-on-noise this project has hit twice. A flip now needs the pair to clear the floor in *both* orderings; both verdicts are carried. **Separate live bug: NLL is numerically broken on MC and ensemble** (1.9e16 VIS MC, 2.4e13 IR MC vs 5.99 for the VIS σ-head) — near-zero σ. It went NO-SIGNAL so no verdict moved, but any pooled NLL from those arms is meaningless |
| 2026-09-03 | 1 (data) | **Night labels reverted by an unknown writer, then recovered** — `filter_night_boxes.py --restore` + `restore_night_perbox.py --execute --force` | working tree | 7,591 of 17,502 `pohang01` train label files were byte-identical to their `.pre_visfilter` backups at 21:19, i.e. pre-filter state; box count had drifted 94,553 → 113,325 (+18,772). After recovery: **10,766 files rewritten, 38,135 dropped, 94,553 kept, `pohang01` train 96,584 boxes, all-VIS hash `b92739202127`** — exactly the pre-registered numbers | **A false alarm I raised and must not be repeated: the two label hashes are different SCOPES, not a mismatch.** `b92739202127` is all-VIS (train+val+test, printed by `restore_night_perbox.py`); `8ed69b5974ed` is the train split only (what `verify_dataset_state.py --expect-label-hash` compares). Both correct, not interchangeable. Cause of the revert is **OQ-13**, still open. Also cleared two stale Ultralytics `*.cache` files before training — a cached label scan would have silently trained the recovered run on the old labels |
| 2026-09-03 | 2 | **`gauss_vis_nightfull` launched** — cold start from COCO `yolo26m` on the restored labels, `data_vis_stride2.yaml`, imgsz 640, batch 16, epochs 100, patience 20, seed 0 | prereg `5761e9f`, `scripts/train_night_fullscratch.py` | **RUNNING** (~14 h expected, early stop near epoch 45 by the shipped run's precedent; 2.7 it/s ≈ 43 img/s on the 4080). Gates passed at launch: prereg committed, label hash `8ed69b5974ed` verified, cold start confirmed (756/810 COCO transfer, no `--weights`) | The V1 primary arm. `gauss_vis_nightrestore` is a fine-tune from a checkpoint that encodes "night is empty", which was fine for the *restore* question — the prior only made ALIVE harder to reach — but for the *veto* question the checkpoint is the treatment, and deciding a switch against a handicapped detector repeats the error this project already made once. Writes only `runs/full_scale/gauss_vis_nightfull/` |
| 2026-09-03 | 1 | **VIS benchmark re-scoped: stride 4 kept, epochs 100 → 25, all 93 runs rerun** on dgxanode01 | queue `runs/queue_vis_benchmark_ep25` | Priced against the 93 archived Phase 1 runs. **Best-so-far mAP is flat after ~epoch 10:** truncating at 15 vs 63 gives top-tier spread 0.0147 vs 0.0145, gap-to-`26n` 0.0705 vs 0.0668, seed sd 0.0071 vs 0.0073. `yolo26m` scores 0.3142 at both. **Stride and epochs are the same knob** — cost is samples seen, so stride 4 × 20 ep, stride 6 × 30 ep and stride 8 × 40 ep all cost 133 h; at a fixed budget the *lower* stride wins on unique-frame diversity, so raising stride was rejected. 25 ep = 602k samples vs the ~481k where Phase 1 was already flat | **All 93 rerun, not the 81 remaining:** the 11 completed rows trained at 100 ep and several passed 25, so mixing budgets across rows is not a benchmark. Measured bias up to **+0.0079** against a variant-difference se of 0.0059 and a tier-1/tier-2 gap of 0.0103 — the same size as the effect being reported, and it would land unevenly within a variant. 12 extra runs ≈ 24 h. Old stride-4 outputs stay on disk, simply not pooled. Rate caveat: archive median 81.6 img/s vs 20.5 observed on `yolov9e`, so the 190 h estimate may be ~4x optimistic |
| 2026-08-20 | 3→4 | `eval_final_system.py` (CPU, cached yolo26s predictions, 1000-resample paired bootstrap) — the finalized decision layer measured, plus the soft-weight ablation and the gated-vs-VIS bootstrap (followup open item 1) | this session (D27–D30) | **Finalized system:** night cells at parity with `ir_only` (all CIs span zero; fog/night closed to −0.0001 by dilate-15); **glare/day beats both single streams** (vs VIS +0.0022 [+0.0004, +0.0042]); clean/day is a tie (+0.0006, spans zero). **Ablations:** `no_veto` loses every night cell (−0.0037…−0.0173, CIs exclude zero) — the veto is load-bearing; `no_maha` loses fog/night −0.0028 but gains glare/day +0.0023 — Mahalanobis kept per the pre-registered rule; `cap_only` ≈ `no_maha` (r_box inert). Full tables: `runs/eval/final_system.md` | Also caught + corrected a transcription error in followup §4 (dilate-15 fog/night veto rate is 71%, not 89% — json says 0.7074; mAP 0.0809 reproduced exactly). `s4_vis_rect` still training during this run; GPU untouched. Architecture + code changes: `docs/architecture-final-2026-08-20.md` §7 |
| 2026-07-07 | 2 | `smoke_gaussian` (CPU, synthetic heteroscedastic, yolov8n@320, 10 ep, warmup 2 + ramp 2) | post-`37bac1e` working tree | **GREEN** — nll/epoch [0, 0, 0.06, −0.11, −0.15, −0.17, −0.18, −0.14, −0.15, −0.11]; σ: 4.47px CV 0.42; noise-direction PASS (0.0639 > 0.0538); toy mAP50 0.97 | Negative NLL is correct (log σ² < 0 when σ < 1 stride unit). Deterministic: identical trajectory across re-runs |
| 2026-07-07 | 2 | `smoke_uq_pipeline` (CPU, same weights) | ditto | **GREEN** — O3 separation clean 33.9 / degraded 59.3 (100% > clean p95); λ=2.00, μ_d=48.8, τ=4.68; gate 16/16 frames; empty-frame fallback exercised | Blank-frame R=0 is correct here (blank IS OOD for the toy model) — the real "clear empty sea = reliable" semantics can only be tested on real data with empty-sea frames in the clean fit set |
| 2026-07-10 | 1 (data) | `prepare_pohang.py --execute` (letterbox 31,010 IR 640×512→640×640; re-split both modalities into contiguous per-run ordinal blocks) | working tree | **audit PASS, exit 0** — VIS 104,250/9,579/8,916, IR 25,143/2,119/3,031; 0 dupes, 0 temporal violations; pohang01 night in train+test; per-run guard bands 150–651 (sized to IR density); OOB IR labels clipped; originals archived `infrared_orig/` | Split was delivered frame-interleaved (train N / val N+1) → would have failed audit. Same ordinal cuts on both modalities keep VIS↔IR pairs in one split. pohang04 = VIS-only (no IR). Plan pre-validated vs real `audit_split()` before any write |
| 2026-08-01 | 1 (data) | `prepare_pohang_fullres.py --execute` (build native-resolution twin at `D:\Datasets\Pohang_dataset_full\`) | working tree | **verify PASS, exit 0** (1,097 s) — 158,319 images hardlinked (127,309 VIS 2048×1080 + 31,010 IR 640×512, **0 new image bytes**; D: free 321.3 → 320.8 GB), labels copied, balanced split lists ported verbatim (VIS 96,275/11,352/11,445, IR 23,279/2,234/2,518), night filter ported = 17,502 files / 132,688 boxes (ship 126,948, buoy 5,740). Independent gates: `verify_yolo_format.py` PASS both modalities; `audit_split.py` PASS both (0 dupes, 0 temporal violations); val/test label hashes byte-identical to source | Exists so `imgsz` 1024/1280 runs stay comparable to the 640 grid (OQ-7). **Filter list was PORTED, not recomputed:** `filter_night_boxes.py` uses `PAD_LEVEL=4` but VIS letterboxes pad with 114, so padding was never excluded — 47% of every 640 frame sits at 114 and dominates the median (all four day runs report median exactly 114.0). The effective rule was "content ~95th-pct < 100"; on unpadded native frames a bright day frame measures 33, so re-running the threshold would empty *all* pohang01 train labels. 640 outcome is unaffected. Docs: `D:\...\PREPARATION.md`, `docs/dataset-changes-2026-07.md` §4 |
| 2026-07-07 | 3 | `smoke_phase3` (CPU: MC p=0.15/T=5 + M=2 ensemble @10 ep; 8 caches; full metric/gate/fusion chain) | post-`e75c110` working tree | **GREEN** — all 6 corruptions valid; MC σ CV 1.62, ens σ CV 0.96; metrics finite for all sources incl. DFL row; learned gate 100% clean-preference; systems mAP50-95: vis(blind) 0.000 / ir 0.778 / naive 0.738 / gated 0.739 / learned 0.740; 6-row ablation | Toy-scale notes: (1) MC/ens NLL blow-up (4003/627 vs Gaussian 2.8) = agreeing-passes overconfidence — the contrast Table 2 exists to measure, exaggerated at T=5/tiny models; (2) fully-blind degraded stream → all combination rules converge in the ablation (expected: R≈0 condemns the stream under every rule); differences emerge under partial degradation at real scale. Two smoke-design fixes en route: gate training needs both outcomes represented (interleaved degradation), ensemble smoke CSV must live under the smoke root (resume-skip reused stale members) |
| 2026-08-17 | 1 | **Phase 1 benchmark grid — COMPLETE** (9 variants × 3 seeds on `682dbe9f0f05`, laptop + DGX MIG slice; 66 `pilot` rows merged) | `cc686ee` | **93 rows, 31 variants** in `phase1_benchmark/results.csv`. Seed-mean leader `yolo26x` 0.3049 ± 0.0020, but the top eight span 0.0055 and its margin over `yolov9e` is 0.0016 — **below its own seed sd, so not separable.** Robust finding: capacity floor, every n/t-scale variant 0.2486–0.2567 across five families. 26 of 27 `main` rows admissible | Two defects shape the methods section: **ultralytics 8.4.7 reports mAP ~0.034 higher than 8.4.90 on identical weights** (isolated by re-validating one server checkpoint under both versions; P/R move <0.01%, so it is confined to AP integration), and **two grid processes shared one run directory** for `yolo12x` seeds 0/1 (detected by the epoch column *decreasing* in file order — duplicate-epoch checks miss it). Also: ultralytics rebuilds `EarlyStopping` on resume without restoring it, so patience counts from a local peak; `best.pt` is unaffected. Admissibility rule (≥ patience epochs past own peak) is the sole exclusion criterion. Full account + limitations text: `docs/phase1-experimental-record.md` |
| 2026-08-17 | 2 | **Gaussian σ² head ported to end2end/YOLO26** (D26) + new structural gate | this session | **BOTH §18-2 gates GREEN.** End2end (`yolo26n`, 10 ep, imgsz 320): σ on one2one only (2,4,2100 finite); top-k gather keeps σ with its box for all 10 tagged rows; **detector parity exact, max \|Δ\| 0.00e+00** with NLL fully active; warm-up nll=[0.0, 0.0, 0.0482, 0.0194, −0.0478, …]; σ mean 7.57 px CV 0.291; `dfl_sigma_ltrb` absent as designed; toy mAP50-95 0.537. DFL path re-run unregressed: σ 4.33 px CV 0.364, noise direction PASS (0.0640 > 0.0539), DFL row present (8.14 px) | Bit-identical parity is D17's guarantee *measured*, not assumed. Negative NLL is correct (log σ² < 0 when σ < 1 stride unit). Two harness fixes en route: a `.pt`'s `model.args` is a plain dict and needs `get_cfg(DEFAULT_CFG)` before a criterion can be built; and `clamp_(0, -0.01)` returns the *max*, so the DFL-clamp bug collapses targets to a constant −0.01 rather than to zero. Nothing here resolves OQ-10 — the port is required under both §7.2 options |
| 2026-08-17 | — | Repo cleanup + consolidation | `7d12631` | `main` fast-forwarded to the phase-1 record branch (it was 7 commits behind and, checked out, was a **broken tree** — `src/uqfusion/data/` was empty, so `scripts/audit_split.py` could not import). **8.6 GB reclaimed:** both original server tarballs deleted after file-level verification (696/696 and 2,281/2,281 members matched to extracted counterparts, 140 checksum-verified byte-identical), two grid logs gzipped 435 MB → 39 MB, `__pycache__` and `.ipynb_checkpoints` removed | The two dated Phase 1 handoffs were folded into `docs/phase1-experimental-record.md` (§11 traps, §12 per-seed detail, §16 measured VRAM limits) and deleted; both remain in git history. Also retired by owner decision: the rented-GPU overflow experiment — its toolkit, stale ignore rules, operational logs and the one abandoned run's orphan `last.pt` (338 MB, never had a curve or a CSV row) are all gone. All Phase 1 training is laptop + DGX, as the record states |
